# Sub-task 001 — 宿主工作区面板与插件扩展能力调查

**状态**：待调查
**创建**：2026-08-20
**所属 memorial**：005-workspace-active-inactive-split

## 任务描述

调查 `E:\work\sp\deepseek-harness`（DeepSeek Harness 宿主）的以下事实，结果写回本工单：

1. **工作区面板实现位置**：截图中的左侧栏「工作区」面板（列出 win-shell-mcp、dsh-web-ui、book2、openCodeMM、official-domestic-website、deepseek-harness、未分组，每个工作区下挂会话列表）在代码中哪个包/哪个组件实现？给出文件路径与关键行号。
2. **workspace 实体模型**：workspace 的数据结构是什么？字段有哪些？列表持久化在哪（KV？config 文件？storageDomain？）？是否已有类似「分组」（截图中有「未分组」）的概念，其实现方式？
3. **自定义元数据能力**：workspace 实体是否支持扩展字段/元数据（例如存一个 active/inactive 标记）？宿主有没有提供 plugin 写 workspace 元数据的 API？
4. **插件 UI 注入能力边界**：DSH 插件 client 半区能注入 UI（dsh-web-ui-jx 已有 SidebarEntry 侧边栏先例）。插件能否**隐藏或替换**宿主已有的工作区面板？宿主有没有针对侧栏/工作区面板的扩展点（slot、registry、patch 机制）？检查 `cordis.patch.yml` 相关机制与宿主侧是否有对应支持。
5. **拖拽基建**：宿主代码里是否已有 HTML5 DnD 或拖拽库的现成实现可复用？

## 结果

（待调查后填写）
