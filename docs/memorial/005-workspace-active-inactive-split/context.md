# Memorial 005 — 工作区活跃/不活跃分栏

**状态**：进行中
**创建**：2026-08-20
**slug**：workspace-active-inactive-split

---

## 诉求

用户原话（按提出顺序）：

1. > 现在的工作区，不管是活跃的还是不活跃 都在一起，我想把工作区分成两块，一块是活跃的，一块是不活跃的。用户可以拖动工作目录在两个工作区直接。这样是不是违背了皮肤插件的本意？要不要区分一个项目，还是直接重新做一个工作区面板

2. > 我是想实现这个功能，想想怎么处理，多开一个仓库去管理？

---

## 追问记录

### 2026-08-20 — grill 前观点（供用户确认或纠正）

在 grill 开始前，agent 先给出过一段初步判断，现记录为**待验证假设**：

- 活跃/不活跃分栏属于「数据组织 + 交互逻辑」，不在皮肤插件（dsh-web-ui-jx 纯视觉层）职责内 → 待验证
- 拖拽跨栏是纯交互，与皮肤无关 → 待验证
- 该功能应落在宿主应用（deepseek-harness）或独立插件仓库，不应塞进 dsh-web-ui-jx → 待验证

**事实查证（sub-task/001 进行中）**：宿主工作区面板实现位置、workspace 实体模型是否支持自定义元数据、插件 UI 注入能力边界（能否替换/隐藏宿主面板）。

---

## 决策汇总

（空）

---

## 待澄清

- 宿主 workspace 实体模型：active 标记存在哪？是否支持自定义元数据？
- 宿主工作区面板的实现位置与可替换性（插件能否隐藏/替换宿主面板）？
- 「多开一个仓库」的具体形态：新插件仓库 vs fork 宿主 vs 改宿主主仓？
- 拖拽是 HTML5 DnD 还是宿主已有拖拽基建可复用？
