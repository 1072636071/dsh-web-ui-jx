# 05 — 验收：全量测试、构建与发布检查

**Status:** resolved

**Blocked by:** 01, 02, 03, 04

**构建内容：** 端到端验收本次功能。要点：
1. 新增逻辑补单元测试：循环修复脚本产物复检（首尾缝/爆亮对称）、变体播放列表推进、随机不重复调度、段间停顿、开关回落、打断重抽。
2. 跑全量既有测试，确保无回归。
3. `npm run build`（host + client 双半区）与 `npm run verify`（发布前验收）全绿。
4. 视觉抽检：3 表情倒放回程自然、working 符咒不再凭空冒出、变体轮换衔接无跳变（如条件允许起宿主目检）。

**验收标准：**

- [ ] 新增单测全绿，既有测试无回归
- [ ] `npm run build` 成功产出 `lib/index.js` + `lib/client.js`
- [ ] `npm run verify` 全部检查通过
- [ ] 视觉抽检通过（循环修复 + 变体轮换衔接）

## 评论

（memorial 008 收尾验收；构建约束见 AGENTS.md。）
