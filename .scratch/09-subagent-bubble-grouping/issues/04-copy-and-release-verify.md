# 文案同步与发布验收

**Status:** resolved

**Blocked by:** 03

**构建内容：** 功能收尾发布：设置卡上限配置描述更新为「顶层归组气泡数」语义；三处文档与实现做一致性核对；产物通过发布前验收流程，具备 `npm publish` / 插件安装链路的发布条件。

**验收标准：**

- [x] 设置卡「角色」section 上限描述反映新语义（顶层归组气泡数），数值范围/默认值/持久化不变
- [x] CONTEXT.md（会话气泡列/根祖先/归组气泡/子代理徽标词条）、DESIGN.md §4 会话气泡列条目、ADR-0018 与实现核对一致；发现漂移先修文档再改代码
- [x] npm run build 产出 host/client 双半区产物且 npm run verify 21 项检查全部通过
- [x] 字体 woff2 构建期未解析属预期（运行时由宿主路由服务），不作为失败项

## 评论

（评论与对话历史追加于此，新内容置于最前。）

- **验收结论（2026-08-23，reviewer）：通过，具备发布条件。** T1–T3 产出逐条对照 ADR-0018 D1–D9 与 PRD 验收标准，无规格偏离；发布门禁四连全绿：`npx vitest run` 12 文件 / 247 用例全过、`npm run typecheck` 零错、`npm run build` 双半区产物齐备（lib/index.js 172KB + lib/client.js 122KB 内联 CSS）、`npm run verify` 21/21 通过。woff2 构建期未解析告警按 AGENTS.md 计为预期，不作失败。
- 文案落点：SettingsCard「角色」section 上限描述改为「角色浮层左侧显示的顶层归组气泡数上限，展开的子会话列表不占名额（1-10）」；标签 / aria-label / min-max 常量 / 默认 10 / `localStorage('jx-max-session-bubbles')` 全部未动。
- 文档同步清单：CONTEXT.md「待实施」清零三处——简报列两处（子代理归组词条 + ADR 索引行），第三处为「会话气泡列」词条尾注「归组模型待实施（现状为平铺）」，按验收标准第 2 条词条一致性一并改写为已实施口径；「子代理归组」词条补登**子代理徽标**词汇与孤儿回退边界；ADR-0018 状态行 → 「已接受（grill 会话定稿）。已实施。」；DESIGN.md §4 与实现逐项核对一致（徽标 ▸N/▾N、缩进 12px、连接线、上限只管顶层、current 传播强制展开均相符），无漂移需回改代码。
- 补充裁定落档（一句话口径）：ADR-0020 决策 4 追加 + DESIGN.md §4 同步括注——**分组模型下豁免按组聚合：根本身或任一入选成员等待交互 ⇒ 整组豁免折叠、根气泡朱砂描边（状态点仍表根自身状态）**；与 `buildBubbleGroups()` 的 group.pending 聚合实现及工单01 队长裁定 #5 一致。
- 实现备注（非缺陷）：①工单02 收缩步移除平铺导出 `selectBubbleEntries`，行为语义由 `buildBubbleGroups` 平铺退化路径承载、以测试域 oracle `legacyFlatSelect` 护栏逐条目比对（对齐 PRD 用户故事 19 收敛为单一纯函数）；套件总数 247 较工单01 回执时点的 278 减少，源于该收缩步按设计删除平铺直测块（session-bubbles.test.ts 头注有记），非回归信号。②PRD「解决方案」段仍写上限默认 5，系 grill 后调整前的快照（ADR-0018 背景 / CONTEXT / DESIGN / config 实际均为默认 10），历史文档不改、仅在此记录。
- 遗留观察（超出本单范围）：docs/adr/0006/0007/0008/0009 等更早 ADR 状态行仍标「待实施」（对应功能实际均已上线），建议后续开小工单统一清理。

- 对齐 05 号功能「构建验收单列」惯例；AGENTS.md 要求修完代码必须构建打包验收。
- 决策依据：PRD 实现决策 6/11、补充说明。
