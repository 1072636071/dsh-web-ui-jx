# 01 — webp 动画时长解析与动态播放

**Status:** resolved

**Blocked by:** 无——可立即开始

**构建内容：** 过渡动画按真实时长完整播放。角色浮层状态切换时，过渡段动画不再只播开头几帧就被切走，末帧收尾定格完整可见；通过 zip/目录导入的新素材自动适配播放时长，解析失败时回退兜底时长不卡死。

**验收标准：**

- [ ] 过渡段播放时长来自素材真实时长（解析常规帧与末帧定格时长并求和），非固定 800ms
- [ ] 解析结果按素材缓存，重复播放不重复解析
- [ ] 解析失败或素材不可达时回退兜底时长，播放链路不中断
- [ ] 对现有 46 个素材，解析结果与已知两档（45 帧型 / 75 帧型）一致
- [ ] 过渡段动画实际可见完整播出（含末帧 536ms 定格），不再被提前截断
- [ ] 新解析逻辑为纯函数（输入素材字节，输出时长），不依赖 DOM
- [ ] 通过 `npm run build` 与 `npm run verify`

## 评论

- 回写（2026-08-23）：清点核实已实施——`webp-duration.ts`（ANMF 解析 + 缓存 + 兜底回退），提交 d821110「工单 01」。状态由 ready-for-agent 补记为 resolved。

（来源：PRD 实现决策 6；memorial 004 sub-task/002 已验证解析可行性。）
