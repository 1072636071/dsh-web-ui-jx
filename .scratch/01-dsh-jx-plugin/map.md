# Map — 01-dsh-jx-plugin

来源：`PRD.md`（本目录）+ `docs/memorial/001-dsh-plugin-ui` + ADR-0001（独立 DSH Bundle 插件）+ ADR-0002（官方三层 token 架构）+ `CONTEXT.md` 词汇表。

## 工单前沿

- **可立即开始**：01 插件骨架与安装链
- 01 后并行：02 素材服务路由 / 03 设计令牌基座
- 02+03 后：04 角色浮层最小可用；02 后另支：07 导入 API 与 KV
- 04 后并行：05 状态机 / 06 台词气泡
- 03+07 后：08 管理界面；03 后：09 FX 特效系统
- 08+09 后：10 侧边栏入口与设置卡
- 05+06+08+10 后：11 npm 发布与安装验收

## 已做决策

- 测试 seam：① 宿主 HTTP 路由层（不 mock webServer）；② 浮层状态机（意图驱动）。KV 经打开的 domain 断言。
- 素材本体走文件系统 + 路由；KV 只存元数据。
- 视觉/令牌正确性以 DESIGN.md 人工审查 + 静态原型比对为准，不自动化。
- zip 素材包格式契约随工单 07 实现定稿并回写 ADR。

## 迷雾

- 宿主事件源（助手行为 → 浮层状态意图）的接入点未在 PRD 范围内定型；工单 05 只预留接口。
- npm 发布时素材随包 vs 随仓库的策略待工单 11 定夺。
