# 侧边栏入口与设置卡

**Status:** ready-for-agent

**Blocked by:** 08, 09

**构建内容：** 用户能从宿主侧边栏入口进入插件管理界面；在设置卡中按偏好独立开关各类特效，设置持久化、重启宿主后保持。

**验收标准：**

- [ ] 侧边栏入口项可见，点击进入管理界面（工单 08 产物）
- [ ] 设置卡含五类 FX 的独立开关 UI，写 `localStorage('jx-fx')` 并即时生效
- [ ] 设置卡样式遵循 DESIGN.md 组件结构（基于语义别名 + `--jx-*` 专属轨）
- [ ] 深浅双主题均覆盖；`:focus-visible` 用 `--jx-gold` 描边
- [ ] 官方小鲸鱼 logo 语义正确（collapsed rail 常驻标记 / expanded 品牌饰件）

## 评论
