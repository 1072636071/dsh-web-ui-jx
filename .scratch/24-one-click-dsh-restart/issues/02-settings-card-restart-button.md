# 设置卡「重启 DSH」按钮

**Status:** done

**Blocked by:** 01

**构建内容：** 设置卡底部出现「重启 DSH」按钮，点击即对 host 重启路由发 `POST`，触发宿主优雅退出并被自愈看守自动重起；不加确认弹窗（调试高频点击、重启不丢数据）。

**验收标准：**

- [ ] 设置卡底部动作行出现「重启 DSH」按钮，与「重置浮层位置」并列，复用次要按钮样式 + 分隔线。
- [ ] 点击后 `fetch("/api/dsh-jx/restart", { method: "POST" })`；失败静默（宿主即将退出重起，不报 UI 错误）。
- [ ] 不加确认弹窗。
- [ ] client 组件渲染测试覆盖该按钮存在与可点击（对齐既有 `settings-card.test.ts`）。

## 评论

（评论与对话历史追加于此，新内容置于最前。）