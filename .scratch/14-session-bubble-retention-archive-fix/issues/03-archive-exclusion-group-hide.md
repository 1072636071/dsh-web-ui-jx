# 归档排除无条件生效 + 根归档整组隐藏

**Status:** ready-for-agent

**Blocked by:** 02

**构建内容：** bug 2 完整闭环——在宿主侧边栏归档的会话从气泡列彻底消失，且不受保留模式总开关影响。普通会话归档后气泡消失；多代理工作流的根会话归档视为整条工作流办结，整组气泡消失；若组内还有运行中/等待确认的子代理，组暂留至全部静止后自动消失（活动信号优先，防止活工作流从视野蒸发）。SDK 会话行不受影响，仅本插件投影层隐藏。

**验收标准：**

- [ ] 归档排除脱离总开关：开关关闭时归档会话同样不可见；退化护栏改写为「除归档排除外，开关关 = 与现状逐条目全等」
- [ ] 归组的根被归档且组内无豁免成员 ⇒ 整组不可见；存在运行中/等待交互成员 ⇒ 组暂留，静止后自动落入隐藏分支
- [ ] 纯逻辑测试覆盖组合矩阵：普通会话归档、根归档×有无豁免成员、当前会话被归档等场景
- [ ] 既有「总开关关闭时输出全等」护栏断言按新语义改写并通过（归档排除为唯一例外）
- [ ] 端到端验证：侧边栏归档一条多代理工作流的根 → 归组气泡消失；归档时有子代理仍在运行 → 组暂留、结束后自动消失；关闭保留模式总开关后归档排除依然生效
- [ ] 全量测试通过；`npm run build` + `npm run verify` 验收通过

## 评论

（评论与对话历史追加于此，新内容置于最前。）

---

### 落序说明（2026-08-26）

本票阻塞于 02 的原因是同谓词区域的落序约束：范围过滤入选扩展（02）先落地，归档排除重构与本票的组级判定随后单次重写同一函数，护栏断言只改写一轮。02 与本票无逻辑上的功能依赖。

### 实施记录（2026-08-26，TDD）

- **验收对照**：
  - [x] AC1 归档排除脱离开关：纯逻辑 3 例（静态归档排除/豁免形态可见/记账集合仍被忽略）+ 组件端到端 1 例（开关关时归档 z 不可见、running live 可见）
  - [x] AC2 根归档整组隐藏：纯逻辑 5 例矩阵（无豁免成员隐藏/运行中暂留/等待交互暂留/根自身运行中豁免/成员归档根存活回归钉）+ 组件端到端 2 例（整组消失/子代理运行暂留）
  - [x] AC3 当前会话被归档：纯逻辑 2 例（current 静态归档整组隐藏、开关关 × current 组合）
  - [x] AC4 护栏改写：「context.keepEnabled=false」护栏按「除归档排除外全等」重写并三方比对平铺基准
- **端到端验证**：自动化以 mock workspaces.list（稳定快照引用）驱动 archivedSessionIds；真实链路人工抽查建议：侧边栏归档一条含已完成子代理的工作流 → 气泡列整组消失。
