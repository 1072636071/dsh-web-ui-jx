/**
 * dsh-session-bubble — 会话气泡通用 React 组件库（工单 01 骨架）。
 *
 * 本包仅搭落点：可独立构建（vite lib mode，ESM + CSS 抽取）的空骨架。
 * 真实导出面（归组引擎、投影层、组件、配置操作）随后续迁移工单迁入。
 */
import "./styles/index.css";

/** 占位导出：库版本信息（后续迁移工单替换为真实公共 API）。 */
export const VERSION = "0.1.0";
